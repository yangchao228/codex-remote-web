# 微信公众号宣传稿：把手机变成 Codex CLI 的安全控制面板

## 标题备选

1. 我开源了一个小工具：用手机远程控制电脑上的 Codex
2. Codex CLI 很强，但我给它补了一个手机控制面板
3. 让电脑跑 Codex，让手机只负责指挥：这个开源项目我先做出来了
4. 我把 Codex CLI 搬到了手机上，但没有把电脑权限交出去
5. 一个本地 AI 工作台的起点：手机控制 Codex，电脑负责执行
6. 用手机看 Codex 跑任务，这个需求我终于自己做了
7. 开源一个 Codex 远程控制面板：本机执行，手机查看，随时停止
8. 如果你也经常用 Codex CLI，这个小工具可能会省你很多切换成本
9. 我不想一直坐在电脑前等 Codex，所以做了这个项目
10. 让 AI 编程从“盯着终端”变成“随时掌控”

## 推荐标题

我不想一直坐在电脑前等 Codex，所以做了这个开源项目

## 摘要

手机提交任务，电脑本机执行，Codex 输出实时可见，还能随时停止。

## 正文

先说一个我自己使用 Codex 时很明显的感受：官方工具越来越强，但“手机轻量控制本机 Codex”这个场景还没有变得足够直接。

OpenAI 已经提供了 Codex CLI、Web、IDE 扩展和桌面 App，也开始支持 SSH 远程连接。如果你想在手机上用 Codex，可以走 ChatGPT / Codex 的云端体验；如果你想让电脑本机跑 Codex，可以用桌面 App 或 CLI。

但如果你的需求是：人离开电脑之后，用手机轻量查看本机 Codex 的进度、提交一个任务、必要时把任务停掉，这个场景还没有一个足够直接的官方入口。

我想补的就是这个更轻的入口。

所以我开源了一个小项目：`Codex CLI Remote Control`。

它的功能说起来很简单：让手机浏览器变成电脑上 Codex CLI 的安全控制面板。

你可以在手机上提交任务，看 Codex 的实时输出，必要时一键停止任务，也可以回看最近的执行历史。真正的 Codex 进程仍然跑在电脑本机，日志也留在本机。

项目地址：

https://github.com/yangchao228/codex-remote-web

先看一下实际效果。

这是 LAN 模式下的使用场景：电脑负责运行本地服务和 Codex，手机只负责打开控制面板。

![LAN 模式下手机连接电脑上的 Codex](imgs/r2-upload/lan-mode.jpg)

手机端第一步是配对。你在电脑终端看到 6 位配对码，然后在手机页面输入。配对完成后，手机才可以提交任务。

![手机端配对和连接状态](https://images.reai.group/images/dd93b3bbc3de7e84-1.jpg)

配对后，手机可以选择工作区，输入任务，然后看到 Codex 的流式输出。它不是给你一个完整终端，而是把“提交任务、看输出、停任务”这几个动作做成一个窄控制台。

![手机端提交 Codex 任务并查看输出](imgs/r2-upload/phone-task-output.jpg)

任务跑完之后，也可以回看最近任务和历史输出。这个对长任务很有用：你不用一直盯着电脑，回来以后也能知道发生了什么。

![手机端查看最近任务和状态](https://images.reai.group/images/a4e9becf576ddb05-3.jpg)

我为什么要做这个东西？

因为 Codex CLI 已经越来越像一个真实的本地工作助手了。

以前我们用 AI 写代码，更多是在聊天框里复制粘贴。现在 Codex 可以直接进入项目目录，读代码、改文件、跑测试、解释错误、整理文档。很多任务一旦开始，其实不需要人一直盯着终端。

但问题也出在这里。

你让 Codex 跑一个 review，或者让它修一个测试问题，可能需要几分钟。人可以去喝水、吃饭、走开一下，可你还是会惦记：它跑完了吗？有没有报错？是不是卡住了？要不要停掉？

这就是我做这个项目的起点。

我不需要一个完整的远程终端，也不想把电脑变成一个暴露在网络上的 shell。我真正需要的是一个很窄的控制面板：

- 能提交 Codex 任务
- 能看到实时输出
- 能知道当前状态
- 能停止正在跑的任务
- 能回看历史记录

手机负责控制，电脑负责执行，人保留判断。

这个边界很重要。

很多远程控制工具给的是完整机器入口，能力很强，但风险也更大。这个项目从第一版开始就刻意限制了手机端的权限。

手机不能执行任意 shell 命令，不能选择任意可执行文件，不能读取电脑文件系统，不能传入环境变量，只能在白名单工作目录里提交 Codex 任务。

默认情况下，它只监听本机 `127.0.0.1`。如果你想在同一个 Wi-Fi 下用手机访问，需要显式开启 LAN 模式。即使在 LAN 模式下，配对码也必须从电脑终端读取，网页不会直接把配对码暴露出来。

目前这个 MVP 已经支持：

- 本地 Web 控制台
- 手机浏览器访问
- 6 位配对码认证
- 工作区白名单
- 受控启动 `codex exec`
- SSE 实时输出
- 手机端停止任务
- 最近任务列表
- 历史输出回放
- 本地 JSONL 审计日志
- localhost 和可信 LAN 模式

用法也很直接。

在电脑上启动：

```bash
npm install
npm run build
npm run start
```

如果想在同一 Wi-Fi 下用手机访问：

```bash
npm run start:lan
```

然后打开终端里打印的地址，输入电脑终端显示的 6 位配对码，就可以开始使用。

这个项目现在还很早期，但我觉得它代表了一个很值得继续做下去的方向：本地 AI 工作站。

未来我们可能不会只是在网页里和 AI 聊天。更常见的使用方式会是：电脑上有一组本地 agent，能进入真实项目、真实文件、真实工作流；手机、平板、网页、桌面应用只是不同的控制入口。

你不一定要把所有权限交出去，也不一定要把所有工作都搬到云端。更好的方式可能是：让 AI 在你的本地工作系统里执行，让人通过更轻的界面持续掌控。

这也是我后续会继续更新的方向。

接下来我计划继续做这些能力：

- 常用任务模板：比如一键 review 当前分支、修测试、解释报错、补文档
- 更好的手机阅读体验：长输出折叠、失败状态聚焦、重点信息提取
- 任务历史资产化：让每次 Codex 执行都能搜索、复盘、复用
- 多工作区管理：给不同项目配置不同默认参数和安全策略
- 本地通知：长任务结束后及时提醒
- 安全远程访问：人在外面时，也能通过受控连接访问家里或公司电脑上的 Codex
- 桌面常驻入口：让它更像一个真正可长期使用的本机工具

我会把这个项目当成一个真实使用中的开源产品来迭代，而不只是一个 demo。

如果你正在使用 Codex CLI，或者你也在搭建自己的 AI 工作流，可以先 Star 一下这个项目。后续版本更新、使用经验、安全边界设计、远程访问方案，我会持续写在这个公众号里。

项目地址：

https://github.com/yangchao228/codex-remote-web

欢迎试用，也欢迎提 Issue。尤其欢迎告诉我：你希望手机端还能控制哪些 Codex 任务？

如果你对本地 AI 工作站、Codex CLI、个人工作流、Human3.0 这些方向感兴趣，可以关注这个公众号。后面我会持续更新这个项目从 MVP 到真正可用工具的过程。

## 结尾互动引导

你最希望在手机上远程控制 Codex 做哪类任务？

是代码 review、修 bug、跑测试、写文档，还是查看长任务进度？

可以在评论区告诉我，我会优先把高频场景做成任务模板。

## 配图建议

1. 首图：`docs/imgs/远程操控电脑codex-lan手机模式.jpg`，突出“手机控制，本机执行”。
2. 操作图 1：`docs/imgs/手机1.jpg`，展示配对和连接状态。
3. 操作图 2：`docs/imgs/手机2.jpg`，展示提交任务和查看输出。
4. 操作图 3：`docs/imgs/手机3.jpg`，展示最近任务和历史状态。
5. 结尾图：项目 GitHub README 截图，提醒 Star / Watch。

## 朋友圈文案

我把最近在用的一个小工具开源了：用手机浏览器控制电脑上的 Codex CLI。

手机可以提交任务、看实时输出、停止执行、回看历史；Codex 仍然只在电脑本机运行，日志也留在本机。

它不是远程 shell，而是一个更窄、更安全的任务控制面板。

后续我会继续更新任务模板、移动端体验、安全远程访问和本地 AI 工作站相关能力。对 Codex CLI / AI 工作流感兴趣的朋友可以先 Star，也欢迎关注公众号看后续迭代。
